import { Button, styles } from "@repo/ui";

export default function Page(): JSX.Element {
  return (
    <Button appName="learning" className={styles.button}>
      FOUNDATION PAGE
    </Button>
  );
}
