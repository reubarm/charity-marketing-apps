import { Button, styles } from "@repo/ui";

export default function Page(): JSX.Element {
  return (
    <Button appName="playing" className={styles.button}>
      FOUNDATION PAGE
    </Button>
  );
}
